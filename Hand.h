/**
 * Header file for hand class
 * 
 * There is a HAND, which has a card object and SIZE. Those cards have a SUIT, a VALUE, and an ENHANCEMENT
 * Can I just make HAND a vector with several cards in it? That is probably smarter
 * 
*/

#include <string>

class Hand {

    /**
     * Requires: nothing
     * Modifies:
     * Effects: Creates an Hand class
     */
    Hand();

    /**
     * Requires: nothing
     * Modifies: handSize
     * Effects: Sets the company name and the initial investment
     */
    // explicit Hand(std::vector<Card> &card, int handSize);






};
